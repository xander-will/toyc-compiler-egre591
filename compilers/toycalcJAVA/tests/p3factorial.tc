int factorial(int x, int y) {
	write(y);
	if (x > 1)
		return x * factorial(x-1);
	else
		return 1;
}

int main() {
	int x;
	
	write("Enter a number"); newline; write(">> ");
	read(x);
	
	write("The factorial is ", factorial(x, 7));
	
	return 0;	
}