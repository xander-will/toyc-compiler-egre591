int factorial(int x) {
	if (x > 1)
		return x * factorial(x-1);
	else
		return 1;
}

int main() {
	int x;
	
	write("Enter a number"); newline; write(">> ");
	read(x);
	
	write("The factorial is ", factorial(x));
	
	return 0;	
}