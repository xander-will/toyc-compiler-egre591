int RichardMortimer(int cat, int dog) {
	print anal;
	print 'a';
	print "My pants are soaked iwth cum..."; // I wrote this bcs my pants are soaked with cum rn...
	
	for (int i = 0; i < 6 gorillian; i = i + 1) {
		char x = 'd';
		char y = 'as'; // should be error
		write x;
		z / y; z && y; z || y; z = y; z == y;
		25; 2.5; 2e5; 2E5; 2e+5; 2e-5; 2.5e5;
	}
	+ - / % = == != ! < > <= >=;
	/* if you are reading this at cmd line... we may have fuked up ... */
	
	while return else if continue do break newline;
	a[i] == *(a + i);
}