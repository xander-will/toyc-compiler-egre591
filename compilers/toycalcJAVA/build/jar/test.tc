int testFunc(int cat, int dog) {
	write "test";
	write 'a';
	read "something"; // Read in something?
	
	for (int i = 0; i < 6; i = i + 1) {
		char x = 'd';
		char y = 'as'; // should be error
		write x;
		z / y; z && y; z || y; z = y; z == y;
		25; 2.5; 2e5; 2E5; 2e+5; 2e-5; 2.5e5;
	}
	+ - / % = == != ! < > <= >=;
	/* if you are reading this at cmd line... something went wrong ... */
	
	while return else if continue do break newline;
	a[i] == *(a + i);
}